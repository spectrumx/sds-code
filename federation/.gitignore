# Dev PKI material — generate with scripts/generate-dev-certs.sh

__pycache__/

federation.toml
federation.peer.toml
site.env

# Local live-test secrets only (keep README + env.example tracked)
scripts/local_e2e/.env
