# SeaweedFS S3 credentials — used by deploy.sh to configure the weed shell
# and create the initial bucket.
# These must match the values in gateway/.envs/<env>/sfs.env.
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=admin
AWS_STORAGE_BUCKET_NAME=spectrumx
