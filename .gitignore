# Environment files
.env
.env.example

# SDK files - lock files that shouldn't be committed
sdk/uv.lock

# SDK pyproject.toml changes (if they're local config)
# Uncomment the next line if you don't want to commit SDK changes
# sdk/pyproject.toml
